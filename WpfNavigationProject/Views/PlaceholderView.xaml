<UserControl x:Class="WpfNavigationProject.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="White" CornerRadius="15" Padding="30" Effect="{StaticResource ShadowDepth3}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" 
                       FontSize="40" 
                       FontWeight="Bold" 
                       Foreground="#483C90" 
                       Margin="0,0,0,20"/>
            <TextBlock Text="Este es un contenido de marcador de posición." 
                       FontSize="20" 
                       Foreground="#777777"/>
            <TextBlock Text="Reemplaza esta vista con la interfaz de usuario real de tu sección." 
                       FontSize="16" 
                       Foreground="#999999" 
                       Margin="0,10,0,0"/>
        </StackPanel>
    </Border>

    <UserControl.Resources>
        <DropShadowEffect x:Key="ShadowDepth3" 
                          Color="Black" 
                          Direction="270" 
                          ShadowDepth="3" 
                          BlurRadius="5" 
                          Opacity="0.1"/>
    </UserControl.Resources>
</UserControl>